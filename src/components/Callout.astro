---
interface Props {
  type?: 'info' | 'warning' | 'tip' | 'performance';
  title?: string;
}

const { type = 'info', title } = Astro.props;

const styles = {
  info: {
    container: 'bg-blue-50 dark:bg-blue-950/50 border-blue-300 dark:border-blue-700 text-blue-900 dark:text-blue-200',
    icon: '💡',
    defaultTitle: 'Nota',
  },
  warning: {
    container: 'bg-amber-50 dark:bg-amber-950/50 border-amber-300 dark:border-amber-700 text-amber-900 dark:text-amber-200',
    icon: '⚠️',
    defaultTitle: 'Advertencia',
  },
  tip: {
    container: 'bg-green-50 dark:bg-green-950/50 border-green-300 dark:border-green-700 text-green-900 dark:text-green-200',
    icon: '✅',
    defaultTitle: 'Tip',
  },
  performance: {
    container: 'bg-purple-50 dark:bg-purple-950/50 border-purple-300 dark:border-purple-700 text-purple-900 dark:text-purple-200',
    icon: '⚡',
    defaultTitle: 'Rendimiento',
  },
};

const style = styles[type];
const displayTitle = title || style.defaultTitle;
---

<div class={`not-prose my-6 p-4 border-l-4 rounded-r-lg ${style.container}`}>
  <div class="flex items-start gap-2">
    <span class="text-lg leading-none mt-0.5">{style.icon}</span>
    <div>
      <p class="font-semibold text-sm mb-1">{displayTitle}</p>
      <div class="text-sm leading-relaxed">
        <slot />
      </div>
    </div>
  </div>
</div>
